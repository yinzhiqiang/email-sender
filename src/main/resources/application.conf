akka {
  loglevel = INFO
}

spray.can.server {
  request-timeout = 1s
}

app {
  server{
    host = "localhost"
    ports{
      http = 9000
    }
    path  = ""
  }
  email{
    server{
      host = "localhost"
      port = 25
      auth = false
      ttls = false
    }
  }
}